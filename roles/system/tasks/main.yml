---
- name: Update package cache and upgrade system
  apt:
    update_cache: yes
    upgrade: dist

- name: Install build-essential and base tools
  apt:
    pkg:
      - build-essential
      - fastfetch
      - htop
      - hwloc
      - sudo

- name: Configure sudoers for aleogr
  copy:
    content: "aleogr ALL=(ALL) ALL"
    dest: /etc/sudoers.d/aleogr
    mode: '0440'
