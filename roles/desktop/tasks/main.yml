---
- name: Install minimal GNOME Desktop components
  apt:
    pkg:
      - gnome-shell
      - gnome-terminal
      - gdm3
    state: present

- name: Ensure GDM3 is enabled but NOT started yet
  systemd:
    name: gdm3
    enabled: yes
    state: stopped
