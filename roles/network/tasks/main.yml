---
# 1. Instalação e Preparação (Roda em qualquer lugar)
- name: Install NetworkManager packages
  apt:
    pkg:
      - network-manager
      - network-manager-gnome
    state: present

# 2. Transição de Serviços (Com cuidado para não travar o Ansible)
- name: Ensure NetworkManager is started and enabled
  systemd:
    name: NetworkManager
    enabled: yes
    state: started

# 3. Configurações Avançadas (APENAS para Hardware Real)
# Usamos o bloco 'when' para garantir que Bridges/VLANs não quebrem a rede da VM
- name: Configure High Performance Networking (Physical Hardware Only)
  block:
    - name: Disable legacy networking service
      systemd:
        name: networking
        enabled: no
        state: stopped

    - name: Remove legacy interfaces file
      file:
        path: /etc/network/interfaces
        state: absent

    - name: Create Bridge interface ({{ bridge_name | default('br0') }})
      community.general.nmcli:
        conn_name: "{{ bridge_name | default('br0') }}"
        ifname: "{{ bridge_name | default('br0') }}"
        type: bridge
        stp: no
        state: present
      notify: Restart NetworkManager

    - name: Create VLAN {{ vlan_id }} on {{ physical_interface }}
      community.general.nmcli:
        conn_name: "{{ physical_interface }}.vlan-vm"
        ifname: "{{ physical_interface }}.{{ vlan_id }}"
        type: vlan
        vlanid: "{{ vlan_id }}"
        vlandev: "{{ physical_interface }}"
        master: "{{ bridge_name | default('br0') }}"
        state: present
      notify: Restart NetworkManager

    - name: Bring up connections
      shell: |
        nmcli con up "{{ bridge_name | default('br0') }}"
        nmcli con up "{{ physical_interface }}.vlan-vm"
      changed_when: false

  # ESTA É A CHAVE: Só executa se NÃO for uma máquina virtual
  when: ansible_virtualization_type == 'none'

# 4. Integração com Libvirt (Virtualização)
- name: Configure Libvirt bridge network
  block:
    - name: Generate Libvirt network XML
      copy:
        dest: "/tmp/{{ bridge_name | default('br0') }}.xml"
        content: |
          <network>
            <name>{{ bridge_name | default('br0') }}</name>
            <forward mode='bridge'/>
            <bridge name='{{ bridge_name | default('br0') }}'/>
          </network>

    - name: Define and start Libvirt network
      virt_net:
        name: "{{ bridge_name | default('br0') }}"
        command: define
        xml: "{{ lookup('file', '/tmp/' + (bridge_name | default('br0')) + '.xml') }}"
        autostart: yes
        state: active
  # Também protegemos o Libvirt para não tentar subir uma bridge inexistente na VM
  when: ansible_virtualization_type == 'none'
