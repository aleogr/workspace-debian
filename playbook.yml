---
- name: Configure Debian 13 Workstation
  hosts: workstation
  become: yes
  vars:
    setup_security: true  # Toggle to false for Virtual Machine environments
  roles:
    - system
    - { role: security, when: setup_security }
    - desktop
    - virtualization
    - network
