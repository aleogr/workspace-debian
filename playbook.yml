---
- name: Configure Debian 13 Workstation
  hosts: workstation
  become: yes
  vars:
    setup_security: true
    create_initial_snapshot: true
  roles:
    - system
    - { role: security, when: setup_security }
    - desktop
    - virtualization
    - network
    - { role: snapshots, when: create_initial_snapshot }
